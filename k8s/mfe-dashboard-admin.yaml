apiVersion: apps/v1
kind: Deployment
metadata:
  name: mfe-dashboard-admin
  namespace: techseva
  labels:
    app: mfe-dashboard-admin
spec:
  replicas: 2
  selector:
    matchLabels:
      app: mfe-dashboard-admin
  template:
    metadata:
      labels:
        app: mfe-dashboard-admin
    spec:
      containers:
        - name: mfe-dashboard-admin
          image: techseva/mfe-dashboard-admin:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 80
              protocol: TCP
          resources:
            requests:
              cpu: "50m"
              memory: "32Mi"
            limits:
              cpu: "200m"
              memory: "64Mi"
          readinessProbe:
            httpGet:
              path: /assets/remoteEntry.js
              port: 80
            initialDelaySeconds: 3
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /assets/remoteEntry.js
              port: 80
            initialDelaySeconds: 10
            periodSeconds: 15
      topologySpreadConstraints:
        - maxSkew: 1
          topologyKey: kubernetes.io/hostname
          whenUnsatisfiable: DoNotSchedule
          labelSelector:
            matchLabels:
              app: mfe-dashboard-admin
---
apiVersion: v1
kind: Service
metadata:
  name: mfe-dashboard-admin
  namespace: techseva
  labels:
    app: mfe-dashboard-admin
spec:
  type: ClusterIP
  selector:
    app: mfe-dashboard-admin
  ports:
    - name: http
      port: 80
      targetPort: 80
      protocol: TCP
